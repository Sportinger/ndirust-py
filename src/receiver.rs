// src/receiver.rs

use pyo3::prelude::*;
use ndi; // Use the ndi crate directly

// Placeholder function to register receiver-related Python functions
pub fn register_receiver_functions(m: &PyModule) -> PyResult<()> {
    // TODO: Add receiver functions and classes here
    // Example: m.add_class::<NdiReceiver>()?;
    Ok(())
} 