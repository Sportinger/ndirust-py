// src/sender.rs

use pyo3::prelude::*;
use ndi; // Use the ndi crate directly

// Placeholder function to register sender-related Python functions
pub fn register_sender_functions(m: &PyModule) -> PyResult<()> {
    // TODO: Add sender functions and classes here
    // Example: m.add_class::<NdiSender>()?;
    Ok(())
} 